{
    "name": "statsanytime-trade-bots",
    "private": true,
    "workspaces": [
        "packages/*"
    ],
    "scripts": {
        "prettier": "prettier --write .",
        "prettier-check": "prettier --check ."
    },
    "devDependencies": {
        "prettier": "^3.0.3"
    }
}
